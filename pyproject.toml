[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "sol"
version = "0.1.0"
license = "MIT"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]